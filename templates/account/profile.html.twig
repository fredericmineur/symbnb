{% extends 'base.html.twig' %}

{% block title %}Modify the profile{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modify profile</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Save the modifications</button>
        {{ form_end(form) }}
    </div>

{% endblock %}